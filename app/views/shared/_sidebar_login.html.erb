<section class="box">
  <header>
    <%= t '.header' %>
  </header>
  <%= link_to t('signup'), signup_path, :class => 'btn success' %>
  <%= t 'or' %>
  <%= link_to t('login'), login_path, :class => 'btn' %>
</section>
